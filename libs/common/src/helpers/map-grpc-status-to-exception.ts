import * as grpc from '@grpc/grpc-js';

const {ALREADY_EXISTS,UNAUTHENTICATED } = grpc.status
export const mapGrpcStatusToException = (code:number | string) =>{
    switch (code) {
        case value:
            
            break;
    
        default:
            break;
    }
}